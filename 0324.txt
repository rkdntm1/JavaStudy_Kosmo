접근제어 -> '문', 가시성 / 패키지 -> 대문으로 보자
private 동일 클래스 
default 패키지 (비추)
protected 상속
public 다른 패키지(공용)

접근자(accessor, getter) 설정자(mutator, setter) -> 꼭 필요한 시점에서만 열어준다.
 ㄴ 나중에 클래스를 업그레이드할때 편하다 
 ㄴ 잘못된 값이 넘어오는경우 사전에 차단 가능

생성자 : 절차
ㄴ 기본생성자는 다른 생성자가 생기면 사라진다. 

오버로딩 : 여러가지 형식으로 생성을 시킬 수 있음
ㄴ(여러 종류를 싣는다) , 메소드형식은 같고 인자만 다름

정적멤버 static = 하나	        

내장 클래스 (inner)

super() : 

(조건) ? (참):(거짓)

%02d , a  -> a를 %02d에 두자리로 출력 (int)
%02f , a  -> a를 %02d에 두자리로 출력 (double)
%02s , a  -> a를 %02d에 두자리로 출력 (string)

Call by Value 
         Name
         Reference


Singleterm pettern

문제를 분석 정의 ! -> 클래스 잘찾기
방법) 명사를 찾으시오..
       속성인지 구분
       상속 포함 연관 의존 관계성 찾기
       
오버라이딩 - 재정의하는것

5W1H 


ppt 1 제목
     2 목차
     3 pmd 넣어버리기 ! 